// 一套默认主题以及一套暗黑主题
// 一套默认主题以及一套暗黑主题
export const themes = {
    default: {
        primaryColor: `${74}, ${144},${226}`,
        primaryTextColor: `${220}, ${20},${60}`,
    },
    dark: {
        primaryColor: `${0},${0},${0}`,
        primaryTextColor: `${0},${0},${255}`,
    },
};